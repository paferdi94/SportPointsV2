<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dannyang27.sportpoints">

    <uses-permission android:name="android.permission.INTERNET" />
    >
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.alta" />
        <activity android:name=".activities.alta2" />
        <activity android:name=".activities.PerfilAlta" />
        <activity android:name=".activities.Deportes" />
        <activity android:name=".activities.Correcto" />
        <activity android:name=".activities.TipoUsuario" />
        <activity android:name=".activities.Login" />
        <activity android:name=".activities.Principal" />
        <activity android:name=".activities.ConfiguracionPerfil" />
        <activity android:name=".activities.OpcionesChoser" />
        <activity android:name=".activities.EquipoInfo" />
        <activity android:name=".activities.EventoInfo"></activity>
    </application>

</manifest>