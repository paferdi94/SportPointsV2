<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/equipo_info"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.dannyang27.sportpoints.activities.EventoInfo">

    <SearchView
        android:layout_width="250dp"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_alignParentStart="true"
        android:id="@+id/buscar_evento" />

    <ImageView
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:paddingRight="20dp"
        android:layout_marginLeft="30dp"

        app:srcCompat="@android:drawable/ic_input_add"
        android:layout_toRightOf="@id/buscar_evento"
        android:layout_alignBottom="@+id/buscar_evento"
        android:id="@+id/imageView_evento" />

    <ListView
        android:paddingTop="40dp"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/buscar_equipo"
        android:layout_centerHorizontal="true"
        android:id="@+id/evento_listView" />
</RelativeLayout>